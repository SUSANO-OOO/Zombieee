export const RELEASE_VERSION = "0.8.0";
export const RELEASE_TAG = `v${RELEASE_VERSION}`;
export const RELEASE_LABEL = `Version ${RELEASE_VERSION}`;
export const RELEASE_TITLE = `西新世紀末物語｜アーリーアクセス版 ${RELEASE_VERSION}`;

export const RELEASE_IDENTITY = Object.freeze({
  version: RELEASE_VERSION,
  tag: RELEASE_TAG,
  label: RELEASE_LABEL,
  title: RELEASE_TITLE,
});
