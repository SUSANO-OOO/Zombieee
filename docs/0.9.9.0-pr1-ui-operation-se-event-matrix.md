# Version 0.9.9.0 PR1 — UI operation SE event matrix

Issue #136 is the execution source. PR1 reuses the existing `AudioMixer` and
existing production audio assets; no new audio file is added.

| Operation | Existing path audited | Cue role | Success / reject feedback | Duplicate guard |
| --- | --- | --- | --- | --- |
| Normal button / menu selection | Campaign map, records, personnel, event controls | `selection` | State change is visible; unavailable actions retain disabled state or reject path | One `click` capture; mixer cooldown and max instances |
| Selection | Region, stage, formation preset/unit, supply, outbreak, records tab | `selection` | Selected state / locked or capacity reason | Stable semantic role, no render-triggered playback |
| Decision / start | Title, prepare, start battle, result continuation | `confirm` | Destination or battle start; invalid preparation rejects | One input, parent callback owns result cue |
| Back | Loadout, personnel, outbreak, records, result return | `back` | Previous screen | One input, no retry replay |
| Supply / purchase | Supply choice and successful employment transaction | `purchase` | Caps/payment or employment success; insufficient caps/owned is `reject` | Transaction receipt and mixer dedupe/cooldown |
| Upgrade | Personnel Level transaction | `upgrade` | Level/stat feedback only after durable save; blocked/max/insufficient caps is `reject` | Level receipt, lock per unit, no retry double spend |
| Reward | Stage/outbreak result receipt | `reward` | Existing result reward panel | Existing result receipt; no new reward transaction |
| Deploy / ability | Unit deploy, support selection, manual ability, pause speed/ability controls | `deploy` or `confirm` | Existing battle banner/effect; invalid target/cooldown/condition is `reject` | Existing battle callback guards plus AudioMixer policy |
| Reject | Locked, cap shortage, cooldown, placement, capacity, no target, condition unmet, already owned | `reject` | Short low mechanical cue and existing visible reason | Existing `AudioMixer` cooldown/polyphony; no disabled-button click loop |
| Pause / speed / audio settings | Pause/resume, speed, BGM/SE, graphics and story settings | `selection` / `back` / `confirm` | Existing state label and setting value | Event-time only; no effect-based playback |
| Result actions | Retry and map/mission continuation | `confirm` / `back` | Existing result transition | Existing result/session guards |

## Audio and rights record

- Added audio files: **0**
- Added bytes: **0**
- Source: existing production audio manifest and existing `AudioMixer` assets
- Creator/license/commercial use/modify/redistribute/master SHA/output SHA: unchanged from the existing production audio provenance ledger; no new asset is introduced by PR1.

The WebKit checks in this PR are automated browser checks. They are not a
physical iPhone confirmation.
