# 西新世紀末物語 — 0.6.0製品仕様

更新日：2026-07-15

## 1. 文書の位置づけ

この文書は、0.6.0アーリーアクセス基盤について、プロデューサーが承認した製品要件と、featureブランチ上の実装候補が採用している構成・初期値・検証基準を整理する。

0.6.0は本書更新時点では候補状態であり、`main`への反映、tag、GitHub Release、ChatGPT Sites公開、製品受入を意味しない。現在の公開状態は[PROJECT_STATE.md](PROJECT_STATE.md)、実装順は[PRODUCT_ROADMAP.md](PRODUCT_ROADMAP.md)、個別の実装・テスト・QA・レビュー結果はIssue #29と対象Draft PRを正式な記録先とする。

本書では、情報を次の四区分で扱う。

- **承認済み要件**：0.6.0で成立させる製品仕様
- **候補実装**：現在の作業ブランチでコードとして確認できる方式。受入前の候補
- **暫定値・差し替え可能表現**：試遊、実機QA、将来の正式アート制作で変更可能。ただし、0.6.0候補として要求する品質を免除しない
- **将来範囲**：0.6.0の製品受入対象に含めない要素

## 2. ブランド

### 承認済み要件

- 正式名称：`西新世紀末物語`
- 読み：`にしじんせいきまつものがたり`
- `アーリーアクセス版`はタイトル補助表記であり、正式名称には含めない
- `202X`表記はプレイヤー画面で使用しない
- 0.5.0以前の`ASHFALL OUTPOST`表記、repository名、既存公開URLは履歴として維持する
- `西新世紀末物語`のロゴ表示はタイトル画面に置き、通常の戦闘HUDへ常設しない
- 主要なプレイヤー向け表示は日本語を主体とし、内部識別子の`CRAWLER`、`SCOUT`、`RANGER`、`BREAKER`、`BRAWLER`、`GUNNER`、`MEDIC`をそのまま表示名に使わない
- 0.6.0で`CRAWLER`に相当するプレイヤー向け暫定表示は`移動拠点`とする。stable ID、コード上のkind、履歴文書内の固有用語は英語のままでよい

### 0.6.0候補の表現

0.6.0候補のタイトルは、production key visualと日本語ロゴタイプを組み合わせ、作品の正式な入口として成立させる。CSSだけの仮背景・仮ロゴを主要表現として残すこと、3ステージを同一背景の色替えで済ませること、会話や音響を未完成のプレースホルダーとして扱うことは受入対象外とする。

production key visual、3ステージ専用背景、7シーンの会話、音源は0.6.0候補で本番品質を満たす必須要素である。一方、これらは将来の正式アート制作で差し替え可能なデータ・アセット境界を維持し、将来差し替え可能であることを0.6.0での未完成理由にはしない。パイセンの名前、外見、素手、既存グラフィックは差し替え対象に含めない。

## 3. アーリーアクセスのゲームループ

### 承認済み要件

次の一連の体験を一画面ゲームとして成立させる。

**タイトル → 導入イベント → エリアマップ・ステージ詳細 → 編成 → 戦闘前イベント → 戦闘 → 星評価・報酬 → 戦闘後イベント → エリアマップへ復帰 → 次ステージ解放**

- 序章名は`新たな世界の始まり`
- ステージ順は配列位置だけに依存させず、stable ID、前提、解放条件、次の解放先を持つデータで管理する
- クリア済みステージは再プレイできる
- エリアマップのステージノードへ過去最高星を直接表示する
- 3ステージ完了後、百道浜・福岡タワー方面の異常反応を示す

### 候補実装

- タイトル、会話、エリアマップ、編成、戦闘、リザルトを明示的な画面状態で切り替える
- 導入1シーンと3ステージの戦闘前・戦闘後各1シーン、計7シーンを収録し、通信断から救助、防衛、TAKUYA撃破、百道浜方面の信号までを一続きの群像劇として成立させる
- 会話データと表示を分離し、専用背景、左右立ち絵、名前、役割、表情、テキスト送り、スキップ、ログ、暗転、揺れを宣言的に指定する。横画面で一度に読める短い日本語を用い、水城、黒木、橘、パイセン、白石、大庭、真壁の話し方と判断基準を場面間で一貫させる
- 戦闘中は接敵、味方死亡、感染予兆、物資、航空支援、移動拠点砲撃・危機、TAKUYA出現・激昂・撃破、感染拠点露出、勝敗へ状態連動する短い無線を使用する。同一状態へ2〜3候補を持たせ、優先度、話者・台詞ごとのcooldown、直前候補回避で反復感を抑える
- 将来地点は`通信途絶`、`調査中`、`経路封鎖`、`高危険区域`として表示し、選択不能にする
- 未開始時の主操作は`物語を始める`、開始済みセーブでは`物語を続ける`とし、通常の開始・続行で既存進行を消去しない
- `最初から始める`とマップ上のセーブ初期化は通常の開始・続行から分離し、どちらも確認ダイアログを必須とする

## 4. 最初の3ステージ

| 順序 | stable ID | 表示名 | 種別・目的 | 次の解放 |
|---|---|---|---|---|
| 1 | `stage-nishijin-shopping-street` | 西新商店街 | 拠点攻略。感染拠点を破壊 | 早良区役所、大庭 豪 |
| 2 | `stage-sawara-ward-office` | 早良区役所 | 時間防衛。救援部隊の撤収完了まで180秒防衛 | 西新防衛線・TAKUYA、真壁 玲奈 |
| 3 | `stage-nishijin-defense-line-takuya` | 西新防衛線・TAKUYA | ボス攻略。TAKUYA撃破後に感染拠点を破壊 | 百道浜方面の異常反応 |

- 次ステージの解放条件は直前ステージの星1以上
- 西新商店街は店舗列とアーケード、配送バン、倒れた自転車、焼け跡、破損看板、暖色灯で場所を識別する
- 早良区役所は庁舎、救援テント、救急車、避難誘導路、コーン、土嚢、寒色投光器・警告灯で場所を識別し、敵拠点は攻撃対象ではなく背景扱いにする
- 西新防衛線は感染煙、有刺鉄線、対車両障害物、クレーター、火災、赤色照明で場所を識別し、0.5.0のTAKUYA戦、3レーン、敵拠点、支援、戦場物資の基本進行をステージ定義へ移行する
- 3ステージは同一背景の色替えにせず、背景、ランドマーク、環境物、照明、戦闘跡の複数カテゴリをそれぞれ変える
- 0.6.0候補は西新商店街に`battle-nishijin-shopping-street-v1.webp`、早良区役所に`battle-sawara-ward-office-v1.webp`、西新防衛線に`battle-nishijin-defense-line-v1.webp`を使用し、地名ラベルを隠しても画面だけで場所を識別できることを受入条件とする
- タイトルは`title-key-visual-v1.webp`、作戦準備・エリア情報は`campaign-operations-room-v1.webp`をproduction visualとして使用し、各画面の文字可読性を損なわない暗部・焦点配置を維持する
- TAKUYAは`正体不明の変異種・異常感染者`であり、感染発生原因は断定しない
- 180秒、敵編成、ウェーブ時刻、背景、目的、ボス、報酬、イベントIDはデータから変更できる

## 5. キャラクターと解放

人物の表示名、役割、説明、解放条件は戦闘用の内部kindから分離する。

| 解放 | unit kind | 表示名 | 役割 | 使用スプライト | 外見との整合確認 |
|---|---|---|---|---|---|
| 初期 | `brawler` | パイセン | 格闘家 | `brawler-sprites-v1.png` | 男性的な運動体型、Tシャツ・短パン・運動靴、素手と一致。名前・外見・素材は変更なし |
| 初期 | `scout` | 橘 迅 | 遊撃手 | `scout-sprites-v2.png` | 男性的な若者、軽装フィールドジャケット、赤いスカーフ、バールと機動近接役が一致 |
| 初期 | `ranger` | 黒木 凛 | 射撃手 | `ranger-sprites-v1.png` | 女性的な顔・赤褐色の髪、帽子・背嚢、自動小銃と射撃役が一致 |
| 初期 | `medic` | 白石 直人 | 衛生兵 | `medic-sprites-v1.png` | 男性的な若者、白い上着、救護腕章・医療バッグ、自動小銃と衛生兵が一致 |
| 西新商店街クリア | `brute` | 大庭 豪 | 破砕兵 | `breaker-sprites-v2.png` | 男性的な巨漢、坊主頭・髭、重装防具、大型両手ハンマーと破砕役が一致 |
| 早良区役所クリア | `gunner` | 真壁 玲奈 | 制圧射手 | `gunner-sprites-v1.png` | 女性的な顔・束ね髪、弾帯・ベスト、自動小銃と制圧射撃役が一致 |

非戦闘案内役は水城 奈々（通信・地図・情報分析）とする。黒木 凛は序章の現場案内役であり、作品は固定主人公を置かない群像劇とする。

既存スプライトの原寸監査により、黒木 凛は自動小銃に合わせて射撃手、真壁 玲奈は弾帯と自動小銃に合わせて制圧射手、白石 直人は男性的な外見表現と自動小銃・救護装備に合わせて衛生兵へ整理した。各ユニットは表示名、役割アイコン、武器、攻撃方式、射程、優先対象、配置ヒントを日本語で持つ。パイセンの名前、外見、素手、既存スプライトは変更しない。

通常プレイでは初期4人だけを編成・出撃可能とし、未解放の大庭 豪と真壁 玲奈はカードを無効化して具体的な解放条件を表示する。該当ステージの初回クリア結果では、ユニットと次の作戦区域を日本語名で示す解放演出を行う。localhost限定QAの全解放は画面へQA表示を出し、通常セーブへ永続化しない。

## 6. 星評価と報酬

### 星評価

星は勝敗と戦闘終了時のプレイヤー側移動拠点の残存HP率だけで算出する。敵側感染拠点、敵拠点、`barricadeHp`、撃破数、経過時間などを星評価へ使用しない。

| 結果 | 星 |
|---|---:|
| 敗北、未クリア、残存HP 0% | 0 |
| 勝利かつ残存HP 1%以上70%未満 | 1 |
| 勝利かつ残存HP 70%以上90%未満 | 2 |
| 勝利かつ残存HP 90%以上 | 3 |

- 保存するのは各ステージの過去最高星とし、低い再挑戦結果で下げない
- 1%、70%、90%はデータとして変更できる初期閾値であり、正式バランスの固定値ではない

### 報酬

暫定通貨名は`補給物資`とする。

| ステージ | base reward | 各星の初回到達報酬 |
|---|---:|---:|
| 西新商店街 | 100 | 50 |
| 早良区役所 | 140 | 70 |
| 西新防衛線・TAKUYA | 200 | 100 |

| 今回の星 | 通常クリア報酬倍率 |
|---|---:|
| 星1 | base reward × 1.00 |
| 星2 | base reward × 1.25 |
| 星3 | base reward × 1.50 |

- 初めて到達した星の報酬は各マイルストーンにつき一度だけ付与する
- 初回で星3なら星1・2・3の初回到達報酬をまとめて付与する
- 星1から星3へ更新した場合は、未取得の星2・3分だけ追加する
- リザルトでは今回の星、過去最高星、記録更新、通常報酬、新規星到達報酬、合計を分けて表示する
- 各戦闘開始時に一意な結果レシートIDを発行し、同じIDの星更新、通常報酬、初回星報酬、ユニット・ステージ解放、ローカル保存は一度だけ適用する
- リロード、戻る連打、リザルト再表示、同一結果の再処理後も結果レシート台帳を保持し、過去の同じ結果を再び支払対象にしない
- base reward、倍率、星到達報酬は試遊後に調整する暫定バランス値である

## 7. ローカルセーブ

### 候補実装

- storage key：`nishijin-campaign-v1`
- save schema version：`2`
- 保存対象：物語開始状態、処理済み結果レシート、クリア済みステージ、各ステージ最高星、星到達報酬の受領状態、補給物資、解放済みステージ、解放済みユニット、最後に選択したステージ、BGM・SFX・音量・reduced motion設定
- 表示名ではなくstable IDを保存する
- schemaなし・v0相当の既知aliasを現在schemaへ正規化する
- unknown fieldは認識済み進行を失う理由にしない
- 不正JSON、型不正、範囲外数値は安全な初期値または正規化値へfallbackする
- 読み込み、移行、保存、戦闘結果反映はテスト可能な純粋関数へ分離する
- 既知のステージ・ユニット解放はクリア済みステージから再導出し、過去に誤保存されたQA全解放値を通常進行として採用しない

0.6.0の保存は端末・ブラウザ内だけであり、クラウド同期、アカウント、端末間共有は将来範囲とする。

## 8. 戦場、標的、接敵テンポ

### 候補実装

- 960×540、3レーン、一画面戦闘、既存キャラクターの描画寸法を維持する
- 0.5.0基準の拠点間有効距離612に対し、0.6.0候補は765とし、25%拡張する
- 味方目標接触点`x=110`、敵目標接触点`x=875`、味方出撃点`x=205`、敵戦闘開始点`x=805`
- CRAWLER描画境界は`x=-112, width=310`、敵拠点は`x=850, width=184`とし、拠点の一部を画面外へ置いて戦場を確保する
- 速度、射程、出撃位置、拠点位置は共通geometry/configへ集約する
- 拡張分を無意味な徒歩時間やキャラクター縮小だけで処理しない

通常攻撃は次の制約を持つ。

- 近接は同一レーンかつ攻撃距離内だけ
- 遠距離は同一レーン優先。隣接レーンは役割設定、射程、明示的な射線成立時だけ
- 2レーン離れた通常攻撃は禁止
- 攻撃開始時に標的IDを固定し、弾道、着弾、ダメージ対象を一致させる
- 回復・支援役は味方集団から過度に離れない
- 死体は標的・移動障害・衝突対象にしない

## 9. 死亡、感染、焼却

### 敵

敵は`alive → dying → corpse → ashing → removed`で遷移する。HP 0以降は非targetable・非blockingとする。

| 区分 | dying | corpse | ashing |
|---|---:|---:|---:|
| 通常 | 0.42秒 | 4.8秒 | 1.45秒 |
| 重装 | 0.68秒 | 6.2秒 | 1.9秒 |
| ボス | 1.15秒 | 8秒 | 2.5秒 |

表示上限は全体36体、1レーン14体とし、画面外または超過した古い非戦闘状態を安全に除去する。これらの秒数と上限は実ブラウザのテンポ・性能確認で調整可能な候補値である。

### 味方

味方は`alive → dying → ally corpse → infection warning → generic zombie`で遷移する。

- ゾンビ化まで12秒
- 残り6秒から軽い警告、残り3秒から強い警告
- 上記は内部タイマーだけに保持し、戦闘画面へ秒数、数字カウントダウン、円形タイマー、進捗バーを表示しない
- 予兆は死体の痙攣、姿勢変化、皮膚・目の色調変化、煙と控えめな効果音で伝える
- 復活時は汎用ゾンビとなり、元の武器、必殺技、役割能力を継承しない
- 復活直後は0.75秒の起き上がり硬直を持つ
- 死体は非blocking
- 死亡・感染状態は戦闘ローカルであり、再戦、戦闘終了、マップ復帰で破棄する

### 火炎焼却

- 地面上のfire areaが味方死体へ接触した時点で感染タイマーを停止する
- 約2秒の燃焼後に灰となり、復活しない
- 燃焼時間の数値や進捗は表示せず、炎、煙、黒化、灰化のアニメーションだけで伝える
- 死体を直接タップする専用操作にはしない
- 火炎範囲の判定と表示を一致させ、pause中は感染・燃焼タイマーを進めない

## 10. パイセン元素材の保全

パイセンは独自キャラクターとして扱い、外見と元素材を変更しない。

- asset：`public/brawler-sprites-v1.png`
- Git blob SHA：`b7c7aaf75c51c3c59ccbb28776c1c6296d409b1a`
- SHA-256：`C2C8E5FA2241A4841C80CA817AFE6EFEDCB235D0BCCAFABCAD931776A8B7D3C4`
- 画像寸法：2172×724
- unit kind：`brawler`
- sprite：6フレーム、1フレーム362×724
- 通常描画destination：62×99

0.6.0候補の死亡表現は元素材を再設計・上書きせず、既存フレームをCanvas上で回転・透明度変更する非破壊fallbackを使う。元素材のworktree外バックアップとハッシュ一致の実施記録はIssue #29を正式所有元とし、バックアップをrepositoryへcommitしない。

## 11. iPhone Safariとvisual viewport

### 承認済み要件

- iPhone横画面でゲームルートを実際の表示可能領域の中央へ置き、左右どちらにも偏らせない
- 不自然な左右の黒い余白をなくし、ゲーム全体を単純縮小して解決しない
- SafariのブラウザUI表示中も、主要HUD、ユニットボタン、物資ボタンを表示可能領域内へ収める
- `100vh`固定に依存せず、safe areaとvisual viewportの変化へ追従する
- PC横画面を壊さない

### 候補実装

- viewport metaへ`viewport-fit=cover`を指定する
- `visualViewport`が使える場合は`width`、`height`、`offsetLeft`、`offsetTop`をCSS変数へ同期し、使えない場合は`window.innerWidth`／`innerHeight`へfallbackする
- `resize`、`orientationchange`、visual viewportの`resize`／`scroll`、Canvasの`ResizeObserver`で再計算する
- ルートはvisual viewportの幅・高さ・オフセットへ固定し、`100dvh`はJavaScript同期前の補助fallbackに限る
- `safe-area-inset-left/right/top/bottom`はゲーム全体を縮める外側余白ではなく、上部情報、下部操作、マップ、編成、会話など個々のUIへ内側余白として適用する
- Canvasは960×540を縦横同倍率でcoverし、中央オフセットを使う。入力座標にも同じscaleとoffsetの逆変換を使う
- 高さ390px以下と350px以下の横画面ではHUDとキャンペーン画面を再構成し、文字と操作領域を保つ
- 幅の広い短い横画面ではキャラクター縮尺を下げず、3レーン中心を短い表示領域用の配置へ切り替える。回転・resize時は出撃済みユニット、死体、戦場物、効果、出撃待ちも同じレーンへ再配置する
- 新規配置する戦場物資、その回復・燃焼範囲、航空支援の着弾判定にも現在のレーン中心を使用し、短い横画面と標準画面の往復後も表示位置と当たり判定を一致させる

### 実ブラウザで証拠を残す検証基準

この文書の記載だけをQA完了の証拠にしない。Issue #29と対象Draft PRへ、少なくとも次を画面寸法・スクリーンショット・計測値とともに記録する。

1. PC 1280×720以上での表示と操作
2. 844×390横画面での左右余白量と中央配置
3. SafariブラウザUI表示を模した低いvisual viewport（例：844×340）での上下の切れ
4. 下部の全ユニット・物資ボタンが表示され、操作できること
5. 縦向きから横向きへ回転した直後の再配置
6. Safari UI表示中と縮小後に相当する二つの高さでの比較
7. タイトル、マップ、編成、会話、戦闘、リザルトの文字切れ・重なり
8. console warning/error 0件

左右余白量、中央差、上端・下端、各ボタン矩形は実測し、単なるページ表示や844×390の静的エミュレーションだけで完了扱いしない。

## 12. 0.6.0本番品質の視覚・物語・音響

この章の要素は「将来制作する正式素材」ではなく、0.6.0候補がDraft PRの停止地点までに満たす必須品質とする。featureブランチ上で実装・検証できたことと、`main`反映・公開・製品受入が完了したことは区別する。

### 視覚と物語

- production key visual、日本語ロゴタイプ、作戦室ビジュアル、3ステージ専用背景を主要画面へ実際に接続し、CSS図形や旧共通背景は読み込み失敗時のfallbackに限る
- 導入と全3ステージの戦闘前・戦闘後、計7シーンを本編として収録する。通信、救助対象、仲間の合流、戦闘目的、戦闘後の損失、次の異常反応を、説明文だけでなく人物の判断と応答で描く
- 戦闘無線はゲーム状態から発火し、各状態に複数候補を持つ。通常プレイへQA文言や英語の内部kindを露出させず、短い日本語、話者名、優先度、cooldownで戦場を妨げない
- 感染進行と焼却は内部タイマーを維持してよいが、台詞を含むプレイヤー向け表現へ残り秒数や数値進捗を出さない。痙攣、姿勢、皮膚・目、煙、炎、灰化と非数値の短い警告音・無線で伝える
- production visualと会話データは将来の正式アート・演出へ差し替え可能にする。ただし差し替え可能性は、0.6.0候補での画質、場所の識別、可読性、物語完成度を下げる理由にしない

### 音楽と効果音

- タイトル、導入、エリアマップ、3ステージ戦闘、TAKUYA戦、勝利、敗北に実音源のBGMを割り当て、場面遷移で自然にcrossfadeする。主要BGMを発振器だけで代用しない
- 武器は素手、バール、ハンマー、自動小銃、制圧連射、移動拠点砲撃を聞き分けられる実素材SFXへ接続する
- 人間の非言語音は男性軽量、男性重量、女性の区分を持ち、攻撃・被弾・死亡をそれぞれ複数候補から直前反復なしで再生する。6ユニットの体格、性別表現、武器、役割に合う区分を使用する
- 敵は`walker`、`runner`、`spitter`、`crusher`、`shade`、`abomination`、`turned`、`takuya`ごとに攻撃・被弾・死亡の音群を分け、各群に複数候補を持たせる
- 感染予兆、着火・燃焼、無線開閉・ノイズ、戦場物資、航空支援、UI操作も実素材へ接続する。Web Audioによる生成音は、素材取得・decode失敗時にもゲーム進行を止めない限定的なfallbackに留める

### AudioMixer

0.6.0候補は共通`AudioMixer`を音響の入口とし、次を満たす。

- ユーザー操作後のAudioContext解放と、解放前に要求されたsceneの安全な保留
- scene単位のpreload・lazy load、BGM crossfade、会話・主要効果音中のducking
- BGM・SFXの有効状態と音量をセーブ設定へ反映し、mute、pause、再戦、マップ復帰でも二重再生しない
- round-robin、shuffleまたはrandom poolで直前候補を避け、cooldown、priority、polyphony、cueごとの同時数上限を適用する
- scene遷移、画面復帰、component破棄時に音源nodeと参照を停止・解放し、無限音、重複loop、listener・node leakを残さない
- 欠落・decode失敗は同一内容を無制限にconsoleへ出さず、fallbackと診断可能な上限付き警告へ処理する

### 権利と検証

- 採用する生成画像、音楽、効果音は`THIRD_PARTY_ASSETS.md`の権利台帳へ、最終ファイル単位で作者・配布元・URL・ライセンス・商用利用・改変・再配布・クレジット条件・取得日・SHA-256・加工内容・配置先を記録する
- ライセンス不明、転載元だけで原著作者・利用条件を追跡できない、または商用利用・再配布可否を確認できない素材は採用しない
- PC横画面、844×390、Safari UI表示相当の844×340、回転直後で、画像の欠落、音源404、console warning/error、二重・無限再生、主要UIの切れがないことを確認する
- 自動テスト、Lint、diff-check、本番build、CIを成功させ、視覚、物語、音響、ライセンス、モバイルを独立観点でレビューする。High／Medium指摘はDraft PR停止前に解消する
- 物理iPhoneの内蔵スピーカー・イヤホンでの聴感、実端末負荷、発熱、長時間動作はローカルのブラウザQAで代替できないため、0.6.0候補の未確認事項として明記し、製品受入前の実機確認対象とする

## 13. 調整可能・将来範囲

### 0.6.0で試遊後に調整できるもの

- 星閾値、base reward、倍率、星到達報酬
- ウェーブ、敵数、180秒防衛の設定値
- 接敵テンポを損なわない範囲の速度・出撃位置・射程
- 死体残存、灰化、感染警告、燃焼の秒数と表示上限
- production visual・会話・音響の構造を壊さないHUD余白、文字サイズ、タップ領域、音量、crossfade、ducking、cooldown
- iPhone Safariと実機で確認した細かな描画・音響パラメータ

### 0.6.0の製品受入外

- パイセン以外の現行キャラクターアートを置き換える将来の正式キャラクターアート制作
- 序章を超える長大なシナリオ、感染原因の確定、ストーリー分岐の完成
- ユニット購入、本格的な7〜8枠編成、プリセット、育成、装備、図鑑
- アカウント、クラウドセーブ、端末間同期
- 4〜5レーン、複数移動拠点、チャレンジモード
- `main`反映、tag、Release、Sites公開は別のリリース承認工程であり、Draft PR上の候補実装だけでは完了しない
