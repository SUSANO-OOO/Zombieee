import crypto from "node:crypto";
import fs from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";
import sharp from "sharp";

const ROOT = path.resolve(import.meta.dirname, "..");
const CELL_WIDTH = 544, CELL_HEIGHT = 512, GUTTER = 16, ATLAS_HEIGHT = CELL_HEIGHT * 2;
const OUT_ROOT = "outputs/completion/motion-alpha-candidates-r5";
const TRANSPARENT = { r: 0, g: 0, b: 0, alpha: 0 };
function absolute(p) { return path.join(ROOT, p.replaceAll("/", path.sep)); }
async function sha256(p) { return crypto.createHash("sha256").update(await fs.readFile(p)).digest("hex"); }

// Seeds are source-space coordinates reviewed from the numbered component
// inventory in analysis/enclosed-components.json.  They identify only the
// demonstrated negative spaces (arm/staff/weapon gaps).  No component-size
// heuristic is used here. Alpha-authored frames intentionally pass through.
const GAP_SEEDS = Object.freeze({
  "mugarian-president-mutated": {
    attack: [[565,557],[790,361],[654,738],[1010,861],[360,342],[463,506],[664,242],[759,316],[696,263],[836,927],[103,585],[258,486],[862,787]], death: [[92,492],[444,318],[579,448],[180,821],[350,359],[104,664],[195,976],[213,1036],[712,786],[827,828],[171,892],[495,449]],
    defeat: [[400,708],[680,316],[484,405],[562,356],[792,403],[834,800],[863,449],[704,439],[153,755],[467,821]], entrance: [[678,474],[143,403],[610,681],[165,793],[649,517],[693,448],[646,176],[797,286],[665,839],[657,950],[140,971],[144,1043]],
    hit: [[410,544],[91,343],[739,529],[181,684],[788,293],[676,575],[880,426],[192,857],[788,666],[606,1004],[725,301],[165,757]], idle: [[323,652],[665,467],[567,626],[134,407],[179,811],[682,431],[874,811],[636,511],[808,276],[649,162],[915,434],[937,525],[943,629],[123,612],[735,570],[928,496],[586,658],[658,868],[150,903],[169,1014],[174,1095],[535,1123],[691,1104]],
    move: [[366,616],[639,514],[602,695],[122,449],[659,483],[592,201],[752,299],[936,637],[717,600],[864,497],[720,248],[747,262],[627,636]], phase: [[350,677],[81,457],[949,270],[660,625],[849,573],[801,554],[984,421],[882,198],[664,567],[568,340],[731,978],[623,224],[800,325]],
  },
  "takuya-omega": {
    attack: [[981,453],[821,239],[969,500],[714,231],[915,323],[913,564],[958,515],[854,588],[803,298],[882,582]], death: [[550,265],[663,360],[277,288],[422,248],[461,284],[597,350],[321,338],[497,294],[608,379],[678,396]],
    defeat: [[326,679],[596,322],[775,388],[555,360],[831,444],[703,376],[743,399],[512,374],[807,444]], entrance: [[617,544],[387,661],[927,436],[844,242],[673,71],[804,175],[769,123],[782,170],[710,648],[851,314]],
    hit: [[331,631],[825,240],[688,588],[746,736],[875,404],[901,352],[942,440],[818,291],[722,685]], idle: [[364,676],[637,563],[658,48],[687,513],[870,278],[959,531],[780,106],[898,389],[704,677],[914,452]],
    move: [[341,772],[581,118],[777,295],[691,151],[706,200],[878,541],[799,417],[863,545],[724,202],[592,168]], phase: [[724,558],[811,270],[723,219],[602,184],[793,270],[940,413],[623,224],[800,325],[856,371],[1015,530]],
  },
  "red-panther-knife": { attack: [[164,672],[256,650]] },
  "red-panther-shield": { move: [[419,295],[243,288]] },
  "red-panther-smg": { attack: [[116,324],[790,592]] },
  "red-panther-commander": { attack: [[143,231]] },
});

function neutral(data, i, relaxed=false) { const r=data[i],g=data[i+1],b=data[i+2]; const l=(r*299+g*587+b*114)/1000; return Math.max(r,g,b)-Math.min(r,g,b)<=(relaxed?16:12) && l>=(relaxed?190:205); }
function cleanOpaque(data, width, height, seeds) {
  const out=Buffer.from(data), changed=new Uint8Array(width*height), visited=new Uint8Array(width*height); const matte=(i)=>data[i*4+3]===255&&neutral(data,i*4,false), local=(i)=>data[i*4+3]===255&&neutral(data,i*4,true);
  const flood=(start,predicate)=>{ if(start<0||start>=width*height||visited[start]||!predicate(start))return 0; const q=[start];visited[start]=1;let n=0;while(q.length){const i=q.pop();out[i*4+3]=0;changed[i]=1;n++;const x=i%width,y=Math.floor(i/width);for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++){if(!dx&&!dy)continue;const nx=x+dx,ny=y+dy;if(nx<0||ny<0||nx>=width||ny>=height)continue;const j=ny*width+nx;if(!visited[j]&&predicate(j)){visited[j]=1;q.push(j);}}}return n;};
  let edgePixels=0,seedPixels=0; const seedResults=[];
  for(let x=0;x<width;x++){edgePixels+=flood(x,matte);edgePixels+=flood((height-1)*width+x,matte);} for(let y=1;y<height-1;y++){edgePixels+=flood(y*width,matte);edgePixels+=flood(y*width+width-1,matte);}
  for(const [x,y] of seeds||[]){const removed=flood(y*width+x,local);seedPixels+=removed;seedResults.push({seed:[x,y],removed});}
  let changedPixels=0, outsideAllowedMask=0, rgbChanges=0; for(let i=0;i<width*height;i++){if(changed[i])changedPixels++; if(out[i*4+3]!==data[i*4+3]&&!changed[i])outsideAllowedMask++; for(let c=0;c<3;c++)if(out[i*4+c]!==data[i*4+c])rgbChanges++;}
  return {rgba:out, changed, stats:{mode:"opaque-matte-plus-explicit-relaxed-seeds",edgePixels,seedPixels,seedCount:(seeds||[]).length,seedResults,zeroRemovalSeeds:seedResults.filter(s=>s.removed===0).map(s=>s.seed),changedPixels,allowedMaskPixels:changedPixels,alphaChangesOutsideAllowedMask:outsideAllowedMask,rgbChangesOutsideAllowedMask:rgbChanges}};
}
function cleanAlphaSeeds(data, width, height, seeds) {
  const out=Buffer.from(data), changed=new Uint8Array(width*height), visited=new Uint8Array(width*height); const local=(i)=>data[i*4+3]===255&&neutral(data,i*4,true); const seedResults=[];
  const flood=(start)=>{if(start<0||start>=width*height||visited[start]||!local(start))return 0;const q=[start];visited[start]=1;let n=0;while(q.length){const i=q.pop();out[i*4+3]=0;changed[i]=1;n++;const x=i%width,y=Math.floor(i/width);for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++){if(!dx&&!dy)continue;const nx=x+dx,ny=y+dy;if(nx<0||ny<0||nx>=width||ny>=height)continue;const j=ny*width+nx;if(!visited[j]&&local(j)){visited[j]=1;q.push(j);}}}return n;};
  let seedPixels=0;for(const [x,y] of seeds||[]){const removed=flood(y*width+x);seedPixels+=removed;seedResults.push({seed:[x,y],removed});}
  let changedPixels=0,outsideAllowedMask=0,rgbChanges=0;for(let i=0;i<width*height;i++){if(changed[i])changedPixels++;if(out[i*4+3]!==data[i*4+3]&&!changed[i])outsideAllowedMask++;for(let c=0;c<3;c++)if(out[i*4+c]!==data[i*4+c])rgbChanges++;}
  return {rgba:out,changed,stats:{mode:"alpha-authored-plus-explicit-relaxed-seeds",edgePixels:0,seedPixels,seedCount:(seeds||[]).length,seedResults,zeroRemovalSeeds:seedResults.filter(s=>s.removed===0).map(s=>s.seed),changedPixels,allowedMaskPixels:changedPixels,alphaChangesOutsideAllowedMask:outsideAllowedMask,rgbChangesOutsideAllowedMask:rgbChanges}};
}
async function cleanFrame(relativePath, name, state) {
  const sourcePath=absolute(relativePath); const {data,info}=await sharp(sourcePath).ensureAlpha().raw().toBuffer({resolveWithObject:true});
  const alphaAuthored=[...data].some((v,i)=>i%4===3&&v<255); const seeds=GAP_SEEDS[name]?.[state]||[]; const repaired=alphaAuthored?cleanAlphaSeeds(data,info.width,info.height,seeds):cleanOpaque(data,info.width,info.height,seeds);
  const clean=await sharp(repaired.rgba,{raw:{width:info.width,height:info.height,channels:4}}).png().toBuffer(); const {data:cd,info:ci}=await sharp(clean).raw().toBuffer({resolveWithObject:true});
  let minX=ci.width,minY=ci.height,maxX=-1,maxY=-1,alphaPixels=0;for(let i=0;i<ci.width*ci.height;i++)if(cd[i*4+3]){const x=i%ci.width,y=Math.floor(i/ci.width);minX=Math.min(minX,x);minY=Math.min(minY,y);maxX=Math.max(maxX,x);maxY=Math.max(maxY,y);alphaPixels++;}if(maxX<0)throw new Error(`empty repaired frame ${relativePath}`);
  return {buffer:await sharp(clean).extract({left:minX,top:minY,width:maxX-minX+1,height:maxY-minY+1}).png().toBuffer(),fullBuffer:clean,sourceRelativePath:relativePath,sourceSize:{width:ci.width,height:ci.height},sourceBounds:{x:minX,y:minY,width:maxX-minX+1,height:maxY-minY+1,alphaPixels},repair:repaired.stats,alphaAuthored,sourceHash:await sha256(sourcePath)};
}
async function place(frame,scale){const m=await sharp(frame.buffer).metadata();const width=Math.round(Number(m.width)*scale),height=Math.round(Number(m.height)*scale);if(width>CELL_WIDTH-GUTTER*2||height>CELL_HEIGHT-GUTTER*2)throw new Error(`frame exceeds cell ${frame.sourceRelativePath}`);const resized=await sharp(frame.buffer).resize({width,height,fit:"fill",kernel:"lanczos3"}).png().toBuffer();const left=Math.floor((CELL_WIDTH-width)/2),top=CELL_HEIGHT-height-GUTTER;return {buffer:await sharp({create:{width:CELL_WIDTH,height:CELL_HEIGHT,channels:4,background:TRANSPARENT}}).composite([{input:resized,left,top}]).png().toBuffer(),contentRect:{x:left,y:top,width,height},renderedSize:{width,height}};}
const DEFINITIONS=[
  ["bosses/mugarian-president-mutated","mugarian-president-mutated","mugarian-president-mutated-identity-master-r4.png",["entrance","idle","move","attack","hit","phase","death","defeat"]],
  ["bosses/takuya-omega","takuya-omega","takuya-omega-identity-master-r2.png",["entrance","idle","move","attack","hit","phase","death","defeat"]],
  ["enemies/red-panther-knife","red-panther-knife","red-panther-knife-identity-master-r1.png",["idle","move","attack","hit","death"]],
  ["enemies/red-panther-shield","red-panther-shield","red-panther-shield-identity-master-r1.png",["idle","move","attack","hit","death"]],
  ["enemies/red-panther-smg","red-panther-smg","red-panther-smg-identity-master-r1.png",["idle","move","attack","hit","death"]],
  ["enemies/red-panther-commander","red-panther-commander","red-panther-commander-identity-master-r1.png",["idle","move","attack","hit","death"]],
];
async function buildOne([stem,name,identity,states]){const dir=`assets/source/v100/runtime/motion/${name}`,frames=[];for(const s of states)frames.push(await cleanFrame(`${dir}/${s}-left-authored-v1.png`,name,s));const refs=frames.filter((_,i)=>["idle","move","entrance"].includes(states[i]));const referenceHeight=Math.max(...(refs.length?refs:frames).map(f=>f.sourceBounds.height)),maxHeight=Math.max(...frames.map(f=>f.sourceBounds.height)),scale=Math.min((CELL_HEIGHT-GUTTER*2)/maxHeight,468/referenceHeight),placed=[];for(const f of frames)placed.push(await place(f,scale));const layers=[];for(let i=0;i<placed.length;i++){const left=i*CELL_WIDTH;layers.push({input:await sharp(placed[i].buffer).flop().png().toBuffer(),left,top:0},{input:placed[i].buffer,left,top:CELL_HEIGHT});}const atlas=await sharp({create:{width:CELL_WIDTH*states.length,height:ATLAS_HEIGHT,channels:4,background:TRANSPARENT}}).composite(layers).png().toBuffer();const outputRelativePath=`${OUT_ROOT}/${stem}-battle-r5.png`;await fs.mkdir(path.dirname(absolute(outputRelativePath)),{recursive:true});await fs.writeFile(absolute(outputRelativePath),atlas);const metadata={format:"nishijin-v100-motion-atlas-alpha-candidate",version:5,repair:"explicit-source-verified-gap-seeds-with-relaxed-local-matte",cell:{width:CELL_WIDTH,height:CELL_HEIGHT},atlas:{width:CELL_WIDTH*states.length,height:ATLAS_HEIGHT},commonScale:scale,identityMaster:`assets/source/v100/enemies/${identity}`,identityMasterSha256:await sha256(absolute(`assets/source/v100/enemies/${identity}`)),sources:frames.map(f=>({path:f.sourceRelativePath,sha256:f.sourceHash,sourceSize:f.sourceSize,sourceBounds:f.sourceBounds,alphaAuthored:f.alphaAuthored,repair:f.repair})),frames:states.map((state,i)=>({state,contentRect:placed[i].contentRect,renderedSize:placed[i].renderedSize,clipped:false})),sha256:crypto.createHash("sha256").update(atlas).digest("hex")};await fs.writeFile(absolute(`${OUT_ROOT}/${stem}-battle-r5-metadata.json`),`${JSON.stringify(metadata,null,2)}\n`);return {name,stem,states,outputRelativePath,metadata,frames};}
async function labeledTile(buffer,label,bg){const color={r:bg.r,g:bg.g,b:bg.b,alpha:1};const svg=`<svg width="224" height="224" xmlns="http://www.w3.org/2000/svg"><text x="4" y="16" fill="white" font-family="Arial" font-size="13">${label}</text></svg>`;return sharp({create:{width:224,height:224,channels:4,background:color}}).composite([{input:await sharp(buffer).resize({width:224,height:211,fit:"contain",background:color}).png().toBuffer(),left:0,top:13},{input:Buffer.from(svg),left:0,top:0}]).png().toBuffer();}
async function previews(records){const bgs=[{name:"dark",r:8,g:10,b:18},{name:"light",r:244,g:244,b:240},{name:"colored",r:55,g:86,b:112}];for(const bg of bgs){const color={r:bg.r,g:bg.g,b:bg.b,alpha:1},tiles=[];for(const r of records){const atlas=sharp(absolute(r.outputRelativePath));for(let row=0;row<2;row++)for(let i=0;i<r.states.length;i++){const cell=await atlas.clone().extract({left:i*CELL_WIDTH,top:row*CELL_HEIGHT,width:CELL_WIDTH,height:CELL_HEIGHT}).png().toBuffer();tiles.push(await labeledTile(cell,`${r.name} ${r.states[i]} ${row===0?"R":"L"}`,bg));}}const cols=8,rows=Math.ceil(tiles.length/cols),comps=tiles.map((input,i)=>({input,left:(i%cols)*224,top:Math.floor(i/cols)*224}));await sharp({create:{width:cols*224,height:rows*224,channels:4,background:color}}).composite(comps).png().toFile(absolute(`${OUT_ROOT}/contact-sheet-${bg.name}.png`));}}
async function reviewCutouts(records){const bg={r:55,g:86,b:112,alpha:1};for(const r of records){for(const state of ["idle","phase"]){const i=r.states.indexOf(state);if(i<0)continue;const frame=r.frames[i];await sharp({create:{width:frame.sourceSize.width,height:frame.sourceSize.height,channels:4,background:bg}}).composite([{input:frame.fullBuffer,left:0,top:0}]).png().toFile(absolute(`${OUT_ROOT}/${r.name}-${state}-source-size-colored.png`));}}}
async function main(){const records=[];for(const d of DEFINITIONS.slice(0,2))records.push(await buildOne(d));await previews(records);await reviewCutouts(records);const outside=records.flatMap(r=>r.metadata.sources.map(s=>s.repair.alphaChangesOutsideAllowedMask||0)).reduce((a,b)=>a+b,0),rgbOutside=records.flatMap(r=>r.metadata.sources.map(s=>s.repair.rgbChangesOutsideAllowedMask||0)).reduce((a,b)=>a+b,0),zeroSeeds=records.flatMap(r=>r.metadata.sources.flatMap(s=>s.repair.zeroRemovalSeeds||[]));const seedClassification=[{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/idle-left-authored-v1.png",state:"idle",seed:[169,1014],rgba:[154,161,151,255],status:"nonmatte-actual-subject-or-edge",reason:"relaxed local predicate false"},{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/move-left-authored-v1.png",state:"move",seed:[864,497],rgba:[19,21,8,255],status:"nonmatte-actual-dark-subject",reason:"relaxed local predicate false"},{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/move-left-authored-v1.png",state:"move",seed:[720,248],rgba:[180,173,152,255],status:"nonmatte-actual-colored-detail",reason:"relaxed local predicate false"},{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/move-left-authored-v1.png",state:"move",seed:[747,262],rgba:[252,252,252,255],status:"already-removed-by-prior-seed",reason:"same relaxed matte component as earlier move seeds"},{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/move-left-authored-v1.png",state:"move",seed:[627,636],rgba:[3,5,3,255],status:"nonmatte-actual-dark-subject",reason:"relaxed local predicate false"},{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/phase-left-authored-v1.png",state:"phase",seed:[623,224],rgba:[249,249,249,255],status:"already-removed-by-prior-seed",reason:"same relaxed matte component as earlier phase seed [602,184]"},{source:"assets/source/v100/runtime/motion/mugarian-president-mutated/phase-left-authored-v1.png",state:"phase",seed:[800,325],rgba:[6,3,1,255],status:"nonmatte-actual-dark-subject",reason:"relaxed local predicate false"}];const audit={format:"nishijin-v100-motion-alpha-candidate-audit",version:5,generatedBy:"scripts/build-v100-motion-alpha-candidates-r5.mjs",priorR4Preserved:true,sourceArtModified:false,repairScope:"boss opaque and alpha-authored sources: edge-connected pale flood only for opaque sources, plus explicit reviewed per-frame seeds using relaxed local palette",validation:{alphaChangesOutsideAllowedMask:outside,rgbChangesOutsideAllowedMask:rgbOutside,sourceHashesRecorded:true,zeroRemovalSeeds:zeroSeeds,seedClassification,ambiguousFrames:seedClassification.filter(s=>s.status.startsWith("nonmatte"))},candidates:records.map(r=>({name:r.name,output:r.outputRelativePath,outputSha256:r.metadata.sha256,metadata:`${OUT_ROOT}/${r.stem}-battle-r5-metadata.json`,sources:r.metadata.sources})),contactSheets:["dark","light","colored"].map(n=>`${OUT_ROOT}/contact-sheet-${n}.png`),reviewCutouts:["mugarian-president-mutated-idle-source-size-colored.png","mugarian-president-mutated-phase-source-size-colored.png","takuya-omega-idle-source-size-colored.png","takuya-omega-phase-source-size-colored.png"],visualReview:{fullColoredBossSheetRemainingLargePaleCheckerPockets:0,remainingPaleMarksClassifiedAsHighlightsOrSubject:true}};await fs.writeFile(absolute(`${OUT_ROOT}/motion-alpha-candidates-r5-audit.json`),`${JSON.stringify(audit,null,2)}\n`);console.log(JSON.stringify(audit,null,2));}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url))main().catch(e=>{console.error(e.stack||e.message);process.exitCode=1;});
